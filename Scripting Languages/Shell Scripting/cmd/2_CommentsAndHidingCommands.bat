@echo off
REM this commands prevents the command line tool from showing the commands you wrote (and the comments) on the terminal again then the output - it will just give you the output to your command

whoami & REM you can put a comment on the same line as the command like this
REM or you can put a comment on a separate line like this
REM note that when you place a comment on the same line as the command you will need the & operator whose function is to basically concatenate batch commands on the same line

pause