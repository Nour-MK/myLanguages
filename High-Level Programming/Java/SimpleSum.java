public class SimpleSum { // SimpleSum is the name of the program - public class is a java keyword that indicates the header of the program, it must always be followed by the name the user chooses for the program
        
    public static void main (String [] arg) // main is the name of the function - cannot remove the String [] arg or I'll get error
    {
        int sum, x;
        x=1;
        sum=0;
        sum=x+sum;
        System.out.print("The sum is: "+sum);
    }
}
