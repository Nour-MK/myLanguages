//What's the output? ⭐⭐

#include <iostream>
#include <string>
using namespace std;
int main ()
{
string s('a');
cout<<s;
return 0;
}

// Error, because ('a') should actually be a double quotation like this ("a") as a string must always be within
// double quotation marks